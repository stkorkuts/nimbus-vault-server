[package]
name = "nimbus_vault_infrastructure"
version = "0.1.0"
edition = "2024"

[dependencies]
nimbus_vault_domain = { path = "../domain" }
nimbus_vault_application = { path = "../application" }
nimbus_vault_shared = { path = "../shared" }